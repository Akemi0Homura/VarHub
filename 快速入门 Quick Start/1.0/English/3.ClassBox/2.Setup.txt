Since every method in ClassBox has a strict mode passing parameter, the strictModel method in ClassBox is no longer meaningful, so it doesn't do anything in the source code.

The other method, setMode, is exactly the same as Box, so I won't go into that here.